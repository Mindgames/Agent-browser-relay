interface:
  display_name: "Agent Browser Relay"
  short_description: "Relay-backed attached-tab reads and preflight"
  default_prompt: "Use $agent-browser-relay to read, preflight, or troubleshoot an attached Chrome tab through the local relay."
